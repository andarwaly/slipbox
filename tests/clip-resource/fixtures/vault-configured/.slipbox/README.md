# Pending: config.json

Not yet built — depends on the four templates in `../.obsidian/templates/` existing first, since `config.json`'s `templates.*_path` fields point at them. Will also set `paths.resources` to `resources/` (sibling in this fixture vault), filename/frontmatter conventions, and `transcript_languages`.
