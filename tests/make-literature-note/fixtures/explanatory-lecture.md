# Why Boundaries Reduce Change Cost

In this lecture, the instructor explains that a boundary localizes a reason
for change. A payment example shows how a stable interface can protect clients
when an implementation changes. The instructor cautions that boundaries add
coordination cost when introduced without a real variation to isolate.
