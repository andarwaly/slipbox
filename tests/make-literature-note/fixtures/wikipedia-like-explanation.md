# Interface Segregation

Interface segregation is a design principle that favors several focused
interfaces over one broad interface. The idea emerged in object-oriented
design discussions and is commonly illustrated with clients that need only a
subset of operations. The principle does not by itself determine where every
boundary belongs.
