# Doc index

Single global index of every `AGENTS.md` in this repo and what it governs — replaces the old per-file "Child DOX Index" pattern. Update this whenever an `AGENTS.md` is created, deleted, moved, or renamed (see `.agents/dox-framework.md` → Closeout).

```
slipbox/
└── AGENTS.md    ← repo root: purpose, structure, workflow, guardrails for the whole
                     note-taking skill family. Single-bucket repo — no child AGENTS.md
                     exists or is expected under skills/{{skill-name}}/ (see the delta
                     test in .agents/dox-framework.md).
```
